apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    createdBy: gitlab-ce-template
  name: gitlab-redis-data
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 512Mi
